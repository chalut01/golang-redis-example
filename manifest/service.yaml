apiVersion: v1
kind: Service
metadata:
  namespace: redis
  name: golang-redis
spec:
  type: ClusterIP
  selector:
    app: golang-redis
  ports:
    - port: 80
      targetPort: 8080
status:
  loadBalancer: {}